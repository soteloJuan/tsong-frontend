import { ConvertirTotalTiempoPipe } from './convertir-total-tiempo.pipe';

describe('ConvertirTotalTiempoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirTotalTiempoPipe();
    expect(pipe).toBeTruthy();
  });
});
